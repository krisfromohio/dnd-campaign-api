# Required: your Anthropic API key, used only server-side (server/index.js).
# Never put this in client code or commit a real key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model used for NPC generation.
# ANTHROPIC_MODEL=claude-sonnet-4-6
